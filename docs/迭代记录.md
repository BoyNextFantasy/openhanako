# 迭代记录

## 2026-06-29：项目品牌化 — 初始化 Satori

- **分支**：`iter/brand-satori` ✅ 当前就在此分支
- **目标**：将 HanaAgent 重新品牌为 Satori，建立 AI 协作规范基础设施
- **改动**：
  - 创建 `AGENTS.md`、`CLAUDE.md` — AI 入口 + 协作规范
  - 创建 `docs/迭代记录.md`、`docs/迭代计划.md` — 迭代管理
  - `package.json` — name → `satori-cli`, productName → `Satori`, appId → `com.satori.app`
  - 环境变量 `HANA_HOME` → `SATORI_HOME`（全库替换）
  - 文档品牌更新（`doc/启动指南.md` 等）
- **验证**：`npm test` — 23 failures（22 预存 + 1 已修品牌），6941/6974 passed；`npm run typecheck` tbd
- **推送状态**：已推送 ✅（`origin/iter/brand-satori`）
- **合并状态**：未合并（需用户决定是否 squash 到 main）

## 2026-06-30：模块分析 — 全量调研所有待剥离模块

- **分支**：`iter/brand-satori`（沿用同一分支）
- **目标**：对剩余 5 个模块逐一完成功能、影响面、测试覆盖的全面调研

### 完成内容
- 调研 `core/media/`、`core/speech-recognition/`、`lib/channels/`、`lib/bridge/`、`core/computer-use/` 共 5 个模块
- 每个模块记录：功能描述、用户感知变化、Desktop 前端影响面、测试文件清单及测试方法
- 更新 `docs/迭代计划.md` — 从纯二进制改动清单升级为包含完整业务描述的版本
- 更新 `docs/迭代记录.md` — 记录本次调研

### 下一步
等待用户决定：按哪个顺序、跳过哪些、或优先改哪些模块

---

## 2026-06-30：剥离模块 — 删除 plugins/jimeng-cli

- **分支**：`iter/brand-satori`（沿用同一分支）
- **目标**：删除即梦 CLI 插件，验证剥离流程

### 这是什么
即梦 AI 画图插件。AI 通过调用系统命令行 `dreamina` 来生成图片和视频。作为插件通过 bus 事件向 `image-gen` 注册 adapter，与核心引擎无直接 import 耦合。

### 用户能感觉到什么变化
- 删之前：AI 有 `generate_image` / `generate_video` 等画图工具可用
- 删之后：AI 不再提供即梦（Dreamina）相关的图片/视频生成能力

### 改动
- 删除 `plugins/jimeng-cli/` 目录（4 文件）
- 删除专用测试文件 `tests/jimeng-cli-adapter.test.ts`、`tests/jimeng-cli-provider.test.ts`
- 清理 7 个文件中的 jimeng-cli 引用（测试数据替换为 test-* 泛型）
- 删除桌面端 `MediaProviderDetail.schema.test.tsx`（已无测试用例）

### 验证
- **代码残留**：`rg 'jimeng' --glob '!node_modules'` → 0 匹配 ✅
- **全量测试**：`npm test` — 22 failures（同预存，未引入新失败），6923/6955 passed
- **用户亲手测**：启动 `npm run cli`，查看工具列表，不应再出现 jimeng 相关工具

---

## 2026-06-30：剥离模块 — 删除 lib/channels/ 全部代码

- **分支**：`iter/remove-channels`（自 `iter/brand-satori` 分出）
- **目标**：删除多 Agent 群聊系统（channels + DM + agent-phone），约 66 个文件，~14k 行

### 这是什么
多 Agent 群聊系统。Agent 之间创建频道、发消息、@提及，含轮询投递和 phone session 打断回复。包含 `channel-ticker`（轮询引擎）、`ChannelRouter`/`DmRouter`（phone delivery 路由）、`channel-tool`/`dm-tool`（AI 工具）、Desktop 频道面板等。

### 用户能感觉到什么变化
- **AI 工具层面**：`channel` 和 `dm` 工具不再可用
- **CLI 核心**：1v1 用户 ↔ Agent 对话完全不受影响
- **Desktop**：频道标签页、频道消息面板、频道创建弹窗全部消失

### 改动

**Round 1（核心源码，36 个源文件）：**
- 删除 `lib/channels/`（3 文件）、`lib/tools/`（channel-tool + dm-tool）、`core/channel-manager.ts`
- 删除 `hub/`（channel-router + dm-router + agent-executor）、`server/routes/`（channels + dm）
- 删除 `lib/conversations/`（5 个 agent-phone 文件）
- 修复 15 个文件中的引用：`engine.ts`、`agent.ts`、`agent-manager.ts`、`hub/index.ts`、`server/index.ts`、`config-coordinator.ts`、`preferences-manager.ts`、`tool-availability.ts`、`config-schema.ts`、`session-coordinator.ts`、`memory-search.ts`、`session-summary.ts`、`memory-ticker.ts`、`policy.ts`

**Round 2（测试文件，19 个）：**
- 删除所有 channels/DM/agent-phone 相关测试

**Round 3（Desktop，11 个）：**
- 删除 `ChannelsPanel.tsx`、`channel-actions.ts`、`channel-slice.ts`
- 修改 `stores/index.ts`（移除 ChannelSlice 引用）、`ui-slice.ts`（移除 channelCreateOverlayVisible）
- 修改 `SidebarLayout.tsx`（移除 channel inspector 宽度计算）、`InputArea.tsx`/`session-actions.ts`（移除 overlay 检查）
- 修改 `types.ts`（移除 Channel/AgentPhone 相关类型、TabType 移除 `'channels'`）
- 修改 `ws-message-handler.ts`（移除 channel/dm/agent-activity WS 事件处理 + 相关 import）

### 验证
- **`npm run typecheck`** — 仅在预存的 `server/index.ts` 和 `core/migrations.ts` 编码错误上失败（非本次引入）
- **`git diff --stat`** — 66 文件，22 insertions，14161 deletions
- **代码残留**：`rg 'channel\.router|ChannelRouter|channel-manager|channel-tool|dm-tool|agent-phone' --glob '!node_modules'` → 0 匹配 ✅

---

## 2026-07-01：补删 desktop channels 残留 + 默认端口 14500 → 14700

- **分支**：`iter/remove-channels`
- **目标**：修复 channels 剥离后 desktop 端的类型错误；改默认端口避免与官方 HanaAgent 冲突

### 改动
- **desktop 残留清理**（5 文件）：
  - `use-sidebar-resize.ts` — 移除 `currentChannel` + channel inspector 拖拽逻辑
  - `MainContent.tsx` — 简化 `blockChatAttachmentDropOutsideChat`（channels 检查已无用）
  - `MobileApp.tsx` — 移除 `showChannelTabs` + `includeChannels` prop
  - `use-sidebar-resize.test.tsx` — 移除 channel inspector 测试
  - `fresh-compact-maintainer.ts` — 移除 `phoneCompacted` 残留字段
  - `fresh-compact-maintainer.test.ts` — 修复导入已删除模块的引用
- **默认端口**：`14500` → `14700`
  - `core/server-network-config.ts` — `DEFAULT_SERVER_LISTEN_PORT`
  - `desktop/main.cjs` — `listenPort`
  - 27 个测试文件 — 批量替换 `14500` → `14700`
- **持久化配置**：`server-network.json` 中的 `listenPort` 手动改为 `14700`

### 验证
- **`npx tsc --noEmit`** — 0 错误 ✅
- **`npm run build:renderer`** — 通过 ✅
- **端口残留**：`rg '14500' --glob '!node_modules'` → 0 匹配 ✅

---

## 2026-07-01：全代码库功能调研 + 迭代计划重构

- **分支**：`iter/remove-channels`
- **目标**：对我列出的 8 个核心功能迭代方向进行代码调研，评估已有实现 vs 空白，重构 `docs/迭代计划.md` 为双枝干结构

### 调研结果（8 方向）

| 方向 | 已有成熟度 | 结论 |
|------|-----------|------|
| 模式切换 plan/build/target | ⚠️ 半成品 | plan mode 存在但只是 read_only，缺 build/target |
| 权限设计 | ✅ 完整 | 4 层防护，不缺功能，可做减法 |
| 上下文压缩 | ✅ 完整 | 多策略自动选择，生产级 |
| 子 agent | ✅ 完整 | 权限继承/持久化/超时，缺 approval forwarding |
| 只聊天 CLI | ✅ 完整 | WebSocket 流式 TUI，缺 mode flag/diff 显示 |
| 自检纠错复审 | ❌ 空白 | 仅一行 prompt，无系统化机制 |
| 自学习自进化 | ❌ 空白 | 仅被动 fact 记录，无反馈闭环 |
| Loop 工程 | ⚠️ 半成品 | workflow-tool 是 JS 编排，非 AI 原生 plan-execute |

### 改动
- `docs/迭代计划.md` — 重构为双枝干：**模块剥离** + **核心功能迭代**，每个方向含现状评估/缺陷分析/建议迭代步骤
- 迭代计划采用 `[ ]` / `[~]` / `[x]` / `[-]` 标记进度，便于 AI 反复编辑

### 关键文件
- `lib/memory/`（8 文件）— 记忆系统完整调研
- `lib/tools/subagent-tool.ts` — 子 agent 系统
- `lib/sandbox/policy.ts` — 权限系统
- `core/session-compactor.ts` — 上下文压缩
- `core/session-permission-mode.ts` — 模式切换
- `cli/chat.ts` — CLI 聊天模式
- `lib/tools/workflow-tool.ts` — Loop 工程

---

## 2026-07-01：细化自学习自进化方向

- **分支**：`iter/remove-channels`
- **目标**：基于 Hermes Agent 调研结果，将方向 7 从 4 条粗略待办扩展为 5 个子方向（7a-7e）的可执行计划

### 改动
- `docs/迭代计划.md` 方向 7：
  - 新增架构说明（三层学习闭环图）
  - 已有能力盘点（skill 框架、fact-store、experiment registry）
  - 7a **Skill 自动创建**：任务完成钩子、反思 prompt、SKILL.md 生成、渐进式加载、技能自改进
  - 7b **执行轨迹分析**：轨迹记录、失败分类、用户反馈收集、偏好向量化、成功率统计
  - 7c **Curator 后台**：去重/合并、质量评分、过期检测、使用报告、定时调度
  - 7d **GEPA 式自我进化**：轨迹回顾、LLM 变异、实验运行器、自动 PR、安全约束
  - 7e **与现有系统集成**：memory store、experiment registry、session compactor、CLI 命令
  - 设计原则（渐进式、用户可控、低成本、插拔式）
- 更新表格行评估：从"❌ 空白" → "❌ 空白（基础设施就绪）"

### 关键文件
- `docs/迭代计划.md` — 方向 7 从 8 行 → ~70 行

---

## 2026-07-01：开启模式切换迭代

- **分支**：`iter/mode-switching`
- **目标**：实现 plan/build/target 三种模式，system prompt 注入 + CLI flag + 运行时命令
- **状态**：分支已创建，待调研已有代码后开始实现
