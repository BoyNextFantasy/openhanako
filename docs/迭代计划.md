# 迭代计划

> 两个平行枝干：**模块剥离**（减法） + **核心功能迭代**（加法）
>
> `[ ]` = 未开始  `[~]` = 进行中  `[x]` = 已完成  `[-]` = 已取消

---

## 枝干一：无关模块剥离

去掉用不上的模块，保留 coding agent 骨架。按从简单到复杂顺序推进。

### 已完成
- [x] 品牌化（Satori 品牌名、AGENTS.md、CLAUDE.md、package.json）
- [x] 环境变量 `HANA_HOME` → `SATORI_HOME`（34 文件）
- [x] 删除 `plugins/jimeng-cli/` + 清理引用
- [x] 删除 `lib/channels/`（含 Desktop 残留清理）+ 默认端口 `14500→14700`

### 待剥离
- [ ] **`core/media/`** — 图片/视频/音频媒体编排层。改动量 ~15 文件（含 Desktop + 测试）。
- [ ] **`core/speech-recognition/`** — 语音转写。改动量 ~7 文件。
- [ ] **`lib/bridge/`** — Telegram/微信/飞书/钉钉/QQ 多平台 IM 集成。改动量 ~8 源文件 + 大量测试。
- [ ] **`core/computer-use/`** — 桌面 GUI 自动化。改动量 ~10 文件（含 Desktop overlay）。

---

## 枝干二：核心功能迭代

### 已存在评估总览

每个方向的"现状"基于代码调研结果（见 `docs/调研记录.md` 或对应 PRD）：

| # | 方向 | 已有成熟度 | 关键代码位置 | 评估 |
|---|------|-----------|------------|------|
| 1 | 模式切换 plan/build/target | ⚠️ 半成品 | `core/session-permission-mode.ts`, `cli/` | plan mode = read_only 权限模式，无 build/target，CLI 不支持 |
| 2 | 权限设计 | ✅ 完整 | `lib/sandbox/policy.ts`, `path-guard.ts`, `session-permission-mode.ts` | 多层级防护（OS + 路径 + 工具分类 + session 模式），不缺功能，可做减法简化 |
| 3 | 上下文压缩 | ✅ 完整 | `core/session-compactor.ts`, `session-thinking-level.ts` | 多策略自动选择，生产级。桥接 session 的 fresh compact 可改进 |
| 4 | 子 agent | ✅ 完整 | `lib/tools/subagent-tool.ts`, `subagent-run-store.ts`, `subagent-thread-store.ts` | 有权限继承/持久化/超时。缺 approval forwarding（子 agent 向用户请求授权） |
| 5 | 只聊天 CLI | ✅ 完整 | `cli/chat.ts`, `cli/client.ts` | WebSocket 流式 + session 管理 + slash 命令。缺模式切换 flag、diff 显示 |
| 6 | 自检纠错复审 | ❌ 空白 | — | 只有 prompt 启发式语句（`agent.ts:1324`），无重试/验证/自检机制 |
| 7 | 自学习自进化 | ❌ 空白 | `lib/memory/fact-store.ts`（仅被动记录） | 无行为反馈闭环，无 Hermes，无偏好学习 |
| 8 | Loop 工程 | ⚠️ 半成品 | `lib/tools/workflow-tool.ts` | 现有 workflow 是 JS 编排脚本，非 AI 原生 plan-execute 循环 |

### 迭代方向详情

#### 方向 1：模式切换（plan / build / target）

- **已有**：`plan mode` = `read_only` 权限模式，只允许 INFORMATION_TOOLS
- **缺陷**：
  - plan mode 不改变 agent behavior，只是限制了 tool 执行
  - 无 build mode（全力执行）和 target mode（专注特定目标）
  - CLI 完全不支持模式切换
- **建议迭代**：
  - [ ] 定义三种模式语义：`plan`（思考+规划，禁副作用工具）、`build`（全力执行）、`target`（限定目标范围的执行）
  - [ ] system prompt 根据模式注入不同的行为指引
  - [ ] CLI 增加 `--mode plan|build|target` flag + 运行时 `/mode` 命令
  - [ ] 模式持久化到 session metadata

#### 方向 2：权限设计

- **已有**：4 层防护（OS sandbox → 路径守卫 → 工具分类 → session permission mode），1500+ 行
- **缺陷**：
  - 复杂度高，4 层权限叠加难以推理
  - `full-access` 模式绕过所有 sandbox
  - 子 agent 没有 approval forwarding（子 agent 无法向用户请求批准敏感操作）
- **建议迭代**：
  - [ ] 简化/合并冗余的权限层
  - [ ] 子 agent approval forwarding 机制
  - [ ] 用户可配置的默认权限模板

#### 方向 3：上下文压缩

- **已有**：`session-compactor.ts`（689 行）双策略自动选择 + thinking level 控制 + fresh compact
- **缺陷**：
  - 复杂度高，多 provider 多 code path
  - fresh compact 只对桥接 session 每日执行，普通 session 无按需 compact
  - 无预算控制的跨 session 自适应压缩
- **建议迭代**：
  - [ ] 简化 compaction 策略选择逻辑
  - [ ] 普通 session 按需 compact（`/compact` 命令或自动触发）
  - [ ] 全局 token 预算管理

#### 方向 4：子 agent

- **已有**：`subagent-tool.ts`（959 行）+ run store + thread store + 权限继承，生产级
- **缺陷**：
  - 无 approval forwarding（子 agent 需要用户确认时无法传递请求）
  - 无结构化 hub 路由（子 agent 创建/发现的中心化路由）
  - 无并行 fan-out 模式
- **建议迭代**：
  - [ ] approval forwarding 机制
  - [ ] 并行子 agent 执行（`Promise.all` 模式）
  - [ ] 子 agent 结果合并/冲突解决

#### 方向 5：只聊天 CLI

- **已有**：`cli/chat.ts`（338 行）+ client + local-server，完整的流式 TUI
- **缺陷**：
  - 无 `--mode` plan/build flag
  - 无 diff 显示（文件变更摘要）
  - 无 `/fix`、`/explain` 等推理命令
  - 无行内文件编辑审批流程
- **建议迭代**：
  - [ ] 模式切换 CLI 支持
  - [ ] 文件变更 diff 展示
  - [ ] `/fix` `/explain` 等快捷命令
  - [ ] 工具执行确认流程（类似 Codex CLI 的 sandboxed 审批）

#### 方向 6：自检纠错复审

- **已有**：无。仅 `agent.ts:1324` 一行 prompt 指引
- **需要新建**：
  - [ ] 工具执行结果校验层（validate tool output before returning to LLM）
  - [ ] 失败自动重试机制（渐进式 fallback：重试→简化→替代方案）
  - [ ] 复审流程（agent 生成的代码/文件变更自动 review）
  - [ ] 自检 prompt 注入（"检查你的输出是否合理"结构化的 self-check loop）

#### 方向 7：自学习自进化

- **已有**：`fact-store.ts` 被动记录用户偏好 + `experiments/registry.ts` 实验框架
- **需要新建**：
  - [ ] 行为反馈闭环（记录用户对 agent 输出的隐式/显式反馈）
  - [ ] 偏好学习（从用户的修正/拒绝中学习偏好）
  - [ ] Prompt/tool 自动调优（根据成功率调整策略）
  - [ ] Hermes agent 概念研究（自主目标设定 + 执行 + 评估循环）

#### 方向 8：Loop 工程

- **已有**：`workflow-tool.ts`（418 行）JS 编排脚本，支持 `agent()` / `parallel()` / `pipeline()`
- **缺陷**：
  - 需要写 JavaScript，不是 AI 原生 plan-execute 循环
  - 无 session 级别 plan→execute→review 模式
- **建议迭代**：
  - [ ] AI 原生 plan-execute 循环（agent 自动：生成计划 → 逐步执行 → 验证结果 → 调整计划）
  - [ ] Plan 持久化到 session metadata，支持中断恢复
  - [ ] Workflow tool 简化，支持自然语言定义流程

---

### 验收标准

#### 落地成果

- [ ] 减少 token 消耗（对比基线）
- [ ] 做到其他 coding agent（Codex CLI / OpenCode / Claude Code）同类功能
- [ ] 掌握 agent 开发全链路知识

#### 差异化方向

- 选取主流评测基准专注拉出优势（如 SWE-bench、HumanEval、等）
- 主观评测指标（用户体验流畅度、首次使用体验、容错能力）

---

### 处理原则

1. **已有完整实现的方向**（权限/压缩/子agent/CLI）→ 改缺陷、做减法，不重造
2. **半成品方向**（模式切换/loop）→ 补齐缺失结构
3. **空白方向**（自检纠错/自学习）→ 从零设计，小步迭代
4. **每个方向的实现可以是插件**（TS 脚本），别人写的/自己写/AI 写都可以
