# 迭代计划

## 当前迭代：项目品牌化

### 目标
将 HanaAgent 品牌化为 Satori，建立基础设施和协作规范。

### TODO

- [x] 确定品牌名 Satori
- [x] 创建 `AGENTS.md` — AI 入口
- [x] 创建 `CLAUDE.md` — Claude Code 入口
- [x] 创建 `docs/` 目录及管理文档
- [x] `package.json` 品牌化（name: satori-cli, productName: Satori, bin: satori）
- [x] 全库 `HANA_HOME` → `SATORI_HOME` 替换（34 个文件）
- [x] 文档品牌更新（`doc/启动指南.md` 等）
- [x] 修复 `windows-installer-contract.test.ts` 品牌断言
- [x] 验证：`npm test` — 23 failures（22 预存 + 1 已修品牌），6941 passed

## 后续迭代规划

### 剥离模块
- 删除 `lib/bridge/`、`core/computer-use/`、`core/speech-recognition/`、`lib/channels/`、`plugins/beautify/`、`plugins/image-gen/`、`plugins/jimeng-cli/`、`plugins/office/`
- 条件化 `core/engine.ts` 中对应 Manager 创建
- 条件化 `server/index.ts` 路由注册
- 清理 `hub/index.ts` 条件化 ChannelRouter/DmRouter

### 品牌完成
- 数据目录 `.hanako-dev` → `.satori-dev`（需迁移逻辑）
- 更新 README.md / README_EN.md 品牌信息
- 更新 PLUGINS.md / PLUGINS_EN.md / PLUGIN_SDK.md 品牌信息

### 核心优化
- CLI 作为主要入口强化
- Desktop 作为可选保留
- 工具系统（lib/tools/）增强
- 文档完善
