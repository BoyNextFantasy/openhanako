# Satori 面试材料

> 维护原则：这个文件作为后续面试准备主文档。`doc/interview/` 和 `doc/项目亮点简历用.md` 只作为历史参考，后续可以删除。

## 0. 项目亮点

```latex
\datedsubsection{\textbf{Satori} — 桌面端 AI 编程助手，支持长期记忆与主动巡检}{2026.5 - 至今}

\begin{itemize}[parsep=0.2ex]
  \item \textbf{技术栈：}TypeScript / Pi SDK / Electron / React / Hono / SQLite / Vitest / Zustand / Vite
  
  \item \textbf{项目概述：}基于 Pi SDK 构建的本地 Coding Agent，对标 Claude Code/Codex，提供 Electron 桌面端应用；负责将Agent构建成一套安全、可治理、有记忆的完整运行体系，覆盖权限防线、上下文压缩、Plan 模式与Compose工作流编排、定时巡检与长期记忆的设计与验证

  \item \textbf{权限与安全：}设计 5 种权限模式与 5 层安全防线——硬编码命令拦截 → 会话权限判定 → AI 模型安全评审 → OS 系统级沙箱→ 用户确认弹窗

  \item \textbf{上下文管理：}实现三层治理——工具输出修剪（压缩前清除旧轮次大文件读取结果，减少无意义 LLM 调用）→ 缓存保留压缩（在同 cache 前缀上生成结构化摘要，保护 KV cache）→ 硬截断防死锁；增加防抖计数阻断反复压缩，修复摘要衰减导致项目规则/记忆丢失的问题

  \item \textbf{Plan / Compose 工作流：}设计 Plan 模式（只读规划，LLM 先读代码、提问澄清、创建任务树，用户确认后切操作模式执行）；设计 Compose 工作流，基于 Superpowers Skills 模式将复杂迭代拆为结构化管道，执行阶段可并行派遣子 Agent 分头处理独立任务；配套 Question 提问工具和 Task 树形任务管理，持久化避免长任务遗忘进度

  \item \textbf{主动巡检与定时任务：}实现 Heartbeat 心跳巡检和 Cron 定时调度——Agent 每隔固定时间自动检测工作台文件变化、扫描用户在工作目录下放置的任务指令文件，结合长期记忆主动处理待办、整理资料或自主学习；Cron 调度器每分钟扫描到期定时任务并驱动 Agent 后台执行。将 Agent 从被动应答升级为周期性自主巡检 + 按时间表执行

  \item \textbf{记忆系统：}基于 SQLite FactStore 的长期记忆体系，支持会话记忆开关、置顶记忆、可编辑记忆和记忆置顶/删除接口；代码事实优先于记忆，防止过时摘要放大错误
\end{itemize}
```

## 1. 项目介绍

Satori 是一个基于 Pi SDK 构建的本地 Coding Agent，对标 Claude Code / Codex，提供桌面端应用。核心要解决的问题是，一个 Agent 不能只会调工具——它需要安全地操作本地文件、具备规划能力并针对模糊问题会调工具询问，在多轮长对话中不迷失方向、记住跨会话的经验和偏好、甚至在没有用户消息时主动巡检工作区。我负责设计和实现了支撑这套能力的五个核心子系统：**权限防线**、**上下文管理**、**Plan模式 / Compose 工作流**、**主动巡检与定时任务**、**记忆系统**。

## 2. 面试问答

### 一、权限防线

### Q1. ⭐ 5 层防线具体每层做什么？为什么是这个顺序？

第一层硬编码命令拦截在最外层直接拒绝危险命令，比如 `rm -rf`，不需要经过模型判断。第二层会话级权限判定根据当前权限模式过滤可用工具。第三层 AI 安全评审用独立的轻量模型审操作意图，风险未知的标记为需确认。第四层 OS 沙箱通过受限令牌降权，在子进程里隔离执行 shell 命令，限制文件系统访问范围。第五层用户确认弹窗，高风险操作由人拍板。顺序原则是越前置越便宜不经过模型，越后置掌握的信息越多拦截越精准但成本高。

> ⚠️ 注意：第三层 AI 安全评审仅 AUTO 模式下触发

### Q2. AI 安全评审那一层，模型自己审自己，会不会放水？怎么保证它不误判？

评审用的是独立的小模型，不和主模型共用同一套提示词。评审提示词聚焦安全判断不看业务逻辑，输出只分三类：安全放行、不安全拒绝、不确定升级到确认层。这样避免主模型的业务推理干扰安全判断。另外评审只审工具参数不审执行结果——参数是 Agent 可控的所以必审，结果由 OS 沙箱兜底，不需要模型再评一次。如果评审模型输出格式不符合三类标准，默认按"不确定"升级处理，宁可多弹一次确认也不能漏。

### Q3. ⭐ OS 沙箱是怎么实现的？

采用的不是虚拟机方案，而是**受限令牌**——利用系统原生的进程降权机制控制写入范围，只允许 Agent 在工作目录，临时目录内做文件操作，系统目录写不进去。命令执行走独立子进程，环境变量中临时目录和应用数据路径被重定向到沙箱隔离区。另有两道前置防线：命令涉及的文件路径先过目录检查，`rm -rf` 等危险模式在命令黑名单里直接拒绝。核心思路不是信任模型，而是假设命令可能有害、通过多层限制让它做不了坏事。

### 二、上下文压缩

### Q4. ⭐ Context 快满了怎么办？压缩怎么设计？

在 Pi SDK 原生压缩引擎之上叠加了三层治理。

第一层**工具输出修剪**：压缩前先把旧轮次里大文件读取结果清掉——那些内容当时有用过后就是噪声，清除后既能直接缩小上下文，也让后续压缩质量更高。第二层**缓存保留压缩**：在同一个缓存前缀上生成结构化摘要，按任务、文件、过程三个维度组织——记目标、进度、阻塞点和关键上下文，记改了什么文件，记约束、关键决策和失败原因——外加文件操作清单。因为前缀没变，大模型的缓存可以复用，避免了每次压缩都重新编码整个上下文。第三层**硬截断兜底**，压缩后仍超限走截断防死锁。另加两道保险：1.**防抖计数器**在反复压缩时强制阻断循环；2.压缩后从原始来源重新加载项目规则和记忆，防止摘要代际衰减。

### Q4-1. 上下文压缩什么时候触发？

两种情况。一是用户主动调用压缩命令，比如 `/compact`，手动触发即时压缩。二是系统自动检测——Agent 每次模型调用后会计算当前上下文累计消耗的 token 数，当逼近模型上下文窗口限制或预设的安全水位线时，自动触发压缩，把一个已经太长的上下文窗口压回可用范围。

### 三、Plan / Compose 工作流

### Q5. ⭐ Plan 模式和 Compose 工作流是怎么设计的？有什么区别？

Plan 模式和 Compose 解决的是两个不同层面的问题。**Plan 是权限模式**——在工具层限制 Agent 只能读代码、搜文件、跑类型检查这类只读操作，同时设计并开放 Question 提问工具让模型在规划时把模糊需求先问清楚，不能写文件或执行有副作用的命令。模型确认需求后输出结构化计划并创建树形任务清单，用户确认切到操作模式才执行。它的价值是"想清楚了再动手"。

**Compose 是工作流模式**——不限制工具权限，而是基于 Superpowers Skills 模式给模型注入六套结构化技能管道：头脑风暴理清需求、制定实现计划、测试驱动开发、小步执行、代码审查、最终验证，同时可在沙箱里并行派遣子 Agent 处理独立子任务。它的价值是"把复杂迭代约束在可追踪的管道里推进"。

### Q5-1. 多 Agent 怎么协作？怎么避免互相干扰？

Compose 下可并行派遣子 Agent 处理独立任务，两个关键机制：**共享任务树**——子 Agent 读写同一棵树，各自认领节点，主 Agent 通过树的状态聚合进度，避免重复劳动；**权限隔离**——子 Agent 权限永远不能超过父会话，固定边界高于一切权限模式，防止子 Agent 自递归、写长期记忆、对外副作用。会话间状态隔离靠 session 级无状态沙箱，互不持有对方状态。

### 四、主动巡检与定时任务

### Q6. ⭐ Agent 能自己定时醒来干活吗？

做了两套互补的机制。**心跳巡检**——每隔固定时间自动触发，先把工作台文件快照对比出变化，再把变化日志和长期记忆一起喂给模型，由模型自主判断有没有可主动处理的事，比如用户放了任务指令文件或者工作区有资料需要整理。**定时调度**——按用户预设的时间表和任务指令执行，每分钟检查有没有到期的，到了就执行，模型不参与要不要做的决策。核心区别：心跳是模型自己决定做什么，Cron 是按既定时间表做事。

### Q7. 巡检和正常对话冲突吗？如果用户正在聊天，心跳来了会不会打断？

不会。心跳走的是独立后台会话，不和用户的活跃会话共用对话流。用户聊天时心跳只在文件系统层面做快照比对和变更记录，不触发模型推理；等检测到用户空闲后，才把累积的巡检结果喂给模型做决策。两个通道互不阻塞。

### 五、记忆系统

### Q8. ⭐ 记忆系统怎么设计？短期长期怎么分工？

短期记忆在单次会话内生效，核心就三块：对话历史本身由框架维护的消息流；**任务树**结构化记录做了什么、正在做什么、什么被阻塞了；以及每十轮、会话结束和每天定时生成的滚动摘要，提取用户画像事实后编译进长期记忆文件。

长期记忆跨会话共享，基于 **SQLite** 存储。每条记忆是一个元事实，比如"用户偏好xxx"。由模型从定时构建的滚动摘要中批量提取元事实，带标签存入，检索时用标签匹配加 **FTS5** 全文搜索。产物最终编译成记忆文件按需注入系统提示词，让 Agent 跨会话记住用户偏好。此外还有**置顶记忆、可编辑记忆和记忆开关**——用户发现记错了可以直接在对话里纠正，开关关了就不写入不读取，防止污染。

#### Q8-1. 追问和上下文压缩的摘要的回答
上下文快满时另有一套独立的压缩机制，把早期对话压缩为结构化摘要但保留当前目标、已改文件和失败测试不丢，不经记忆系统。

#### Q8-2. 长期记忆按需检索召回吗？什么情况下需要检索？

两条路径。被动注入——每次会话启动时直接注入进系统提示词，Agent 不做任何操作就能看到近期记忆。主动检索工具供模型按需调用，当模型判断当前任务可能涉及历史事实时才触发，用 LLM 生成的标签加 FTS5 关键词去SQLite搜索，不是每次都搜。

#### Q8-3. 长短期记忆是同时注入吗？

是。两者共存于每次对话的上下文窗口——长期记忆在系统提示词里作固定前缀，短期记忆在消息流里随对话增长。

#### Q8-4. 置顶记忆是如何实现置顶的？和长期记忆如何区分开？

普通记忆走自动管线：滚动摘要提取→SQLite→定期合并注入提示词，容量有限可能丢失低频记忆。置顶记忆走独立存储——1.可以是工具直接写入独立的 JSON 文件，全程不经过 SQLite，不截断，合并；2.自动管线生成的记忆也能被用户选择置顶到独立文件。两者在提示词里分层摆放，置顶记忆坐在长期记忆上方，标记为用户要求始终保留，不受管线容量影响。

### Q9. 为什么用 SQLite + FTS5 而不是向量数据库？

SQLite 是嵌入式关系数据库，FTS5 是它内置的全文搜索扩展，能高效做关键词匹配。记忆的存入和检索两端都是 LLM 在生成关键词——存入时从对话提取标签写入，检索时根据当前任务生成查询词用全文搜索匹配。两边的用词习惯天然一致，不需要嵌入模型做语义翻译。v1 用向量方案时发现嵌入偏差反而导致召回不准，v2 切到标签匹配后效果没差但去掉了向量模型的依赖和计算成本。本质不是语义相似度搜索，而是模型自己写的标签自己搜。

### 六、通用架构与工程

### Q10. ⭐ 先整体介绍一下你的本地 Coding Agent 项目，从输入任务到最终修改代码，完整链路是怎样的？

收到任务后分三步：**准备、执行、收敛**。

准备阶段先恢复会话上下文——工作目录、历史对话、权限模式、模型配置、项目规则和相关记忆——然后判任务类型：需求模糊用提问工具先澄清，复杂改动先进 **Plan 只读模式**读代码、拆任务、列风险，用户确认后再切到操作模式执行。执行阶段模型不直接碰文件，而是通过工具层把决策翻译成读文件、搜索代码、编辑、跑测试等动作，每次调用前穿过**权限管道**——Plan 模式只能读，写文件和命令需要对应权限，危险命令直接拒绝。收敛阶段失败不报错退出，错误信息带回下一轮让 Agent 缩小范围重新修复，最终用测试通过、类型检查零错误、人工验收完成闭环。不是一次性撞对，而是**生成→验证→发现问题→带着证据再修→再验证**的收敛循环。

### Q11. ⭐ 你项目里的工具系统是怎么做的？

遵循 Pi SDK 的 工具定义规范。底层由 Pi SDK 转发为模型厂商的工具调用格式。在此基础上工具系统解决四件事：按权限模式和工作流过滤可见工具、失败信息带回下一轮、结果回写对话记录。设计上有两个关键理念——一是渐进式披露，而是根据当前会话模式动态控制可见范围，并且只提供工具描述词；二是工具描述的质量直接决定调用准确率。

### Q11-1. 工具你会用 MCP 吗？还是自己写？大模型工具实现有什么好处？

MCP 是工具/数据源的标准协议，解决工具跨模型、跨 Agent 复用；Function Calling 是模型↔工具的协议。我项目里底层用 Pi SDK 的工具定义规范，内部工具都是自己写的（文件读写、命令执行、搜索、记忆检索），因为内部工具要深度绑定权限、沙箱、审计。好处是结构化 schema——工具名、描述、参数 schema 让模型学会"什么时候调、怎么调"，工具描述质量直接决定调用准确率，所以我把工具描述当一等公民对待。MCP 的价值在生态接入，自己写工具的价值在深度控制。

### Q12. 工具调用失败时，Agent 是直接报错还是做恢复和重试？

关键是区分**可恢复的错误**和**不能绕过的安全边界**，否则重试很容易变成乱试。

可恢复的情况：文件不存在就把错误返回给模型，让它重新搜索路径或者问用户；命令超时保留已有输出，缩小测试范围再跑；测试失败把失败用例和错误栈带回下一轮对话，让模型根据具体错误信息定位修复。不可恢复的情况：权限不够就是安全边界，系统直接硬拦截，弹给用户确认或在只读模式下直接拒绝，不能让模型自己绕过。我的原则就一条：普通错误给模型足够信息让它继续修复，安全错误必须由系统兜底拦截，不能靠模型自觉。

### Q13. ⭐ 支持不同模型后端吗？怎么抽象请求格式、响应结构、错误码和重试逻辑？

支持。核心思路是把**模型标识和模型服务商分开**——同一个模型名可能来自不同供应商，配置里独立记录密钥，模型能力等。利用pi-sdk能力，接入多家时做一层**适配层统一入口**：上层 Agent 只需要知道"我要发一段对话、有哪些工具可以用、要边生成边返回"，不关心底层是 OpenAI 还是 Anthropic 的格式差异，适配层负责把各家格式转成内部统一结构。

### Q14. Agent 反复读同一文件或调同一工具，怎么识别？

从三个层面来看。文件层面，记录最近读过哪些文件以及文件摘要是否变化——如果内容没变，重复读取时可以提醒已有结果。工具层面，记录相近时间段内同一个工具被调用过的参数组合，如果完全重复就提醒避免无效调用。任务层面，用**任务树**结构化记录已经完成什么、正在做什么，减少模型因为忘记进度而去重复操作。

但我不会强行拦截所有重复。比如文件刚被修改后重新读一遍是完全合理的。更好的做法是**提醒加统计**，让 Agent 自己判断要不要跳过，而不是粗暴禁止，否则反而会影响正常的工作流。

### Q15. 评测体系怎么设计？怎么判断改动让 Agent 变强？

做的是轻量评测而不是大规模排行榜，目标不是刷榜，是判断框架改动有没有让 Agent 更稳。

思路是接公开题库——某个公开的编程题本身有测试命令，Agent 负责解题、生成修改，然后跑题目自带的测试来判断过没过。指标分三类：结果指标，效率指标，上下文消耗。

### Q16. 为什么用 Pi SDK 而不是 LangChain 或 LangGraph？

Pi SDK 是开箱即用的 Agent 运行时——会话管理、工具执行、权限管道、流式推送这些能力已经内置好了，我们可以在这个基础上专注做策略层的设计，比如五种权限模式怎么定义、Plan 和 Compose 怎么编排。LangChain/手搓的优势在另一面：你可以精确控制每一步模型调用，出问题时调用栈更透明、更容易定位和修复，基础设施得自己从零搭建。选型本质上是在"基础设施开箱即用"和"执行链路完全可控"之间做权衡——对我们这个需要快速迭代的本地 Agent 场景，Pi SDK 的集成度更匹配。

### Q17. task 工具和任务树是干什么的？

任务树是 LLM 可见的树形工作项，跨轮次不丢失，不占用上下文。 用于Plan 模式下创建计划表，切到操作模式后逐个推进。子 agent 读写同一棵树，实现多 agent 协作拆分。任务状态变化实时推送到 UI，用户能看到进度。



## 3. 场景题专区

> 面试官现在倾向用"现实开发中遇到的问题"来提问，这些场景题从具体情境切入，考查你把系统设计落到真实问题上的能力。回答套路：先给判断和处理思路，再展开系统里哪套机制在兜底，最后接得住追问。

### S1. 长会话失忆

**场景**：用户开了一个 40 轮的长会话。前 20 轮让 Agent 重构了某个模块并改了好几个文件，后 20 轮换了任务。现在用户问"之前重构时那个接口为什么改成回调了？"，Agent 有点答不上来。你会怎么处理？

**思路**：三层机制兜底，Agent 不会真的失忆。第一，**任务树**——结构化记录做了什么、正在做什么、被什么阻塞，跨轮次持久化不丢失，翻任务树就能对齐当时的进度。第二，**上下文压缩摘要**——摘要按任务、文件、过程三维组织，明确记录"改了什么文件"加文件操作清单，压缩后这些上下文仍保留。第三，**工具层**——代码事实优先于记忆，Agent 可以直接重新读目标文件拿最新事实，重复读取还有内容摘要变化检测提醒。

[追问]

> 压缩把细节丢了怎么办？

摘要代际衰减问题专门修复过：压缩后从原始来源重新加载项目规则和记忆；涉及代码的事实直接从代码重新读，不依赖摘要记忆。

### S2. 被提示词诱导执行危险命令

**场景**：用户（或一段精心构造的提示词）诱导 Agent 执行强制推送这种危险操作，比如 `git push origin main --force`，或者 `bash -c "git push -f"` 这种包装形式。系统怎么反应？

**思路**：第一层硬编码拦截直接拒绝，不经过 AI 评审也不弹确认——这类危险变体在白名单之外。破坏性命令（比如递归删除文件）和强制推送的危险变体全部直接拦截；命令解析器处理引号和转义，并递归拆解嵌套的命令包装，防止包装绕过。拦截后返回结构化错误给模型，模型拿到的信息是"该操作被安全策略拒绝"。

[追问]

> 用户真的想强制推送怎么办？

拦截的是 Agent 工具层的行为，用户可以在自己的终端操作；这是设计取舍：宁可误拦不可漏拦，Agent 不提供危险路径。

> 怎么识别包装过的命令？

命令先拆掉外层包装，再对内部命令做同样的黑名单检测，不信任模型自报的命令。

### S3. 子 Agent 并发改同一文件

**场景**：Compose 工作流派了两个子 Agent 并行干活，一个在改 a.ts，另一个也要动 a.ts，或者两个都要改配置文件。会不会互相覆盖、把文件写坏？

**思路**：三层防护。第一，**任务划分**——共享任务树让子 Agent 各自认领节点，主 Agent 按树聚合，设计上尽量让改动面不重叠。第二，**会话级无状态沙箱**——每个子会话独立隔离，不持有引擎级共享状态，互不干扰。第三，**文件级原子写**——真出现同文件竞争时，写入是原子的，不会产生半个文件；一致性由文件系统保证。

[追问]

> 子 Agent 的权限边界？

永远不能超过父会话，固定黑名单防止它自己再造子任务、防止往长期记忆里乱写，这个边界改不了。

### S7. 巡检时用户突然开始提问

**场景**：心跳巡检时间到了，后台 Agent 正在扫描工作区、甚至可能正在写巡检日志，这时用户突然开始聊天提问。两个 Agent 会不会打架、抢上下文、或者互相打断？

**思路**：双通道设计，不会打架。心跳走**独立后台会话**，不共用对话流，互不阻塞。用户活跃时心跳只在文件系统层面做快照比对和变更记录——不触发模型推理；等检测到用户空闲后，才把累积的巡检结果喂给模型做决策。另外连接层面：网络连接断开只中断前台会话，后台巡检和定时任务不受影响，生命周期分离。

[追问]

> 怎么判定"用户空闲"？

不检测在线状态，提示词明确"用户当前没在跟你对话，不要当作用户提问"；调度层判断当前没有正在进行的回答生成，才把巡检结果喂给模型。

> 巡检把自己写的东西当任务怎么办？

跳过自主活动目录，执行后重新比对指纹防止自我触发，5 分钟超时兜底。

### S8. 怎么证明你的改动让 Agent 变强了

**场景**：你说你的压缩、记忆、权限改动让 Agent 变强了。面试官要证据：评测怎么做的？数据怎么来的？会不会是自己给自己打分？

**思路**：第三方裁判，不自判。接公开的编程题库，**题目自带的测试命令是唯一事实来源**，评测只负责：拷贝工作区、跑准备命令、提交任务给 Agent、跑判定测试、记录指标，不自己实现判分器。判定规则三阶段：准备命令通过、Agent 正常跑完、题目自带测试通过，任一失败即任务失败。指标分结果（通过率）、效率（时长/步数）、上下文消耗三类；用**控制变量**证明增量——一次只改一个环节，同一批任务对比通过率和资源消耗。

## 4. 自测题单

> 只列问题，自测用。对着念，自己回答。

> Q1. 5 层防线具体每层做什么？为什么是这个顺序？

> Q2. AI 安全评审那一层，模型自己审自己，会不会放水？怎么保证它不误判？

> Q3. OS 沙箱是怎么实现的？

> Q4. Context 快满了怎么办？压缩怎么设计？

> Q4-1. 上下文压缩什么时候触发？

> Q5. Plan 模式和 Compose 工作流是怎么设计的？有什么区别？

> Q5-1. 多 Agent 怎么协作？怎么避免互相干扰？

> Q6. Agent 能自己定时醒来干活吗？

> Q7. 巡检和正常对话冲突吗？如果用户正在聊天，心跳来了会不会打断？

> Q8. 记忆系统怎么设计？短期长期怎么分工？

> Q8-1. 追问和上下文压缩的摘要的回答

> Q8-2. 长期记忆按需检索召回吗？什么情况下需要检索？

> Q8-3. 长短期记忆是同时注入吗？

> Q8-4. 置顶记忆是如何实现置顶的？和长期记忆如何区分开？

> Q9. 为什么用 SQLite + FTS5 而不是向量数据库？

> Q10. 先整体介绍一下你的本地 Coding Agent 项目，从输入任务到最终修改代码，完整链路是怎样的？

> Q11. 你项目里的工具系统是怎么做的？

> Q11-1. 工具你会用 MCP 吗？还是自己写？大模型工具实现有什么好处？

> Q12. 工具调用失败时，Agent 是直接报错还是做恢复和重试？

> Q13. 支持不同模型后端吗？怎么抽象请求格式、响应结构、错误码和重试逻辑？

> Q14. Agent 反复读同一文件或调同一工具，怎么识别？

> Q15. 评测体系怎么设计？怎么判断改动让 Agent 变强？

> Q16. 为什么用 Pi SDK 而不是 LangChain 或 LangGraph？

> Q17. task 工具和任务树是干什么的？

> S1. 用户开了一个 40 轮的长会话。前 20 轮让 Agent 重构了某个模块并改了好几个文件，后 20 轮换了任务。现在用户问"之前重构时那个接口为什么改成回调了？"，Agent 有点答不上来。你会怎么处理？

> S1追问. 压缩把细节丢了怎么办？

> S2. 用户（或一段精心构造的提示词）诱导 Agent 执行强制推送这种危险操作，比如 git push origin main --force，或者 bash -c "git push -f" 这种包装形式。系统怎么反应？

> S2追问. 用户真的想强制推送怎么办？

> S2追问. 怎么识别包装过的命令？

> S3. Compose 工作流派了两个子 Agent 并行干活，一个在改 a.ts，另一个也要动 a.ts，或者两个都要改配置文件。会不会互相覆盖、把文件写坏？

> S3追问. 子 Agent 的权限边界？

> S7. 心跳巡检时间到了，后台 Agent 正在扫描工作区、甚至可能正在写巡检日志，这时用户突然开始聊天提问。两个 Agent 会不会打架、抢上下文、或者互相打断？

> S7追问. 怎么判定"用户空闲"？

> S7追问. 巡检把自己写的东西当任务怎么办？

> S8. 你说你的压缩、记忆、权限改动让 Agent 变强了。面试官要证据：评测怎么做的？数据怎么来的？会不会是自己给自己打分？